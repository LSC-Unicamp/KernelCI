# ==========================================
# CORE CONFIG: BlackParrot (64-bit)
# ==========================================

cpu_type: "blackparrot"
cpu_variant: "standard"

# Flags de síntese exclusivas deste processador
core_flags: []


# Requisitos mínimos de hardware para este core fazer boot do Linux
requirements:
  min_ram_mb: 32
  needs_opensbi: true